# Example environment file for the Vue frontend
# Copy to `.env` and edit for your environment.

# URL of the backend API (include trailing slash, e.g., http://localhost:5000 or https://<backend-domain>)
VITE_API_URL=http://localhost:5000
